/target
**/*.rs.bk
node_modules/
tools/oracle/dist/
*.duckdb
*.c64retrace
/traces/
*.ppm
tools/oracle/corpus/**/*.out
# scratch goldens — non-deterministic until scenario isolation lands; regenerate locally
tools/oracle/corpus/**/*.golden.json
.DS_Store

# Copyrighted Commodore ROM / commercial-game snapshots — local-only test fixtures, never tracked
crates/trx64-core/tests/fixtures/vsf/*.vsf

# Local working state — agent instructions + build-loop state.
# Kept LOCAL, not published on GitHub. docs/ is TRACKED since 2026-07-02 (the
# design record belongs in the repo); *-loop/ dirs are local loop state.
/CLAUDE.md
/CHECKLIST.md
/loop/
/*-loop/
/dist/

# commercial VICE snapshots (never track)
*.vsf
